class StepError extends Error {
  cause;
  name;

  constructor(message, { cause } = {}) {
    super(message);
    this.name = 'StepError';
    this.cause = cause;
  }
}

module.exports = { StepError };
